{
    "name": "insidious-canoe",
    "version": "1.0.0",
    "scripts": {
        "postinstall": "cd express-node && npm install --only=production && cd ../angular-src && npm install --only=production"
      },
    "config": {
        "unsafe-perm":true
    }
}